Pour se connecter à MongoDB :
Base de données : pc3r-projet
Collection : message

  import "go.mongodb.org/mongo-driver/mongo"
  ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
  defer cancel()
  client, err := mongo.Connect(ctx, options.Client().ApplyURI(
     "mongodb+srv://dbUser:A6zXeHk2SLKjOXaO@cluster0.gf71f.mongodb.net/myFirstDatabase?retryWrites=true&w=majority",
  ))
  if err != nil { log.Fatal(err) }

MySQL :
adresse  : cteillet.ddns.net
port     : 3306
login    : projet-pc3r
password : QXKn6BmR7B7Iwfrs


Exemple connexion en go :
import (
    "fmt"
    "database/sql"
    _ "github.com/go-sql-driver/mysql"
)

func main() {
    fmt.Println("Go MySQL Tutorial")

    // Open up our database connection.
    // I've set up a database on my local machine using phpmyadmin.
    // The database is called testDb
    db, err := sql.Open("mysql", "projet-pc3r:QXKn6BmR7B7Iwfrs@tcp(cteillet.ddns.net:3306)/projet-pc3r")

    // if there is an error opening the connection, handle it
    if err != nil {
		fmt.Println("Probleme")
        panic(err.Error())
    }

	fmt.Println("Pas de probleme")

    // defer the close till after the main function has finished
    // executing
    defer db.Close()

}

Connexion API sportsdata.io
API Key LOL = c86e4989da6247358a15b0c3ab5dbe66

Connexion API pandaScore.co
API Key LOL = 4xg85-0CNl9sOdk-tyFooufCsE8qchuK478B5bUoAOV0j3cREdQ
